[project]
name = "mobius-conf-demo-server"
version = "0.1.0"
description = ""
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "sqlalchemy>=2.0",
    "pydantic>=2.0",
    "websockets>=13.0",
    "httpx>=0.27",
]

[tool.uv]
package = false
